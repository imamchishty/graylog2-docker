# graylog2-docker
graylog2-docker

Currently setup for mac-development.

To start:

`docker-compose up -d`

Go to your browser (admin/admin):

`http://192.168.99.100:9000/`

To stop:

`docker-compose stop`

View logs:

`docker-compose logs`
